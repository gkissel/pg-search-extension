services:
  postgres:
    image: paradedb/paradedb:latest
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: docker
      POSTGRES_DB: app
    volumes:
      - ./data/paradedb_data:/var/lib/postgresql/
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: '1G'


volumes:
  paradedb_data: